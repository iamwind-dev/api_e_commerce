# MongoDB Connection
# Replace with your MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/your-database-name

# JWT Secret Key
# Generate a secure secret using: node -p "require('crypto').randomBytes(64).toString('hex')"
# NEVER use the default value in production!
JWT_SECRET=generate_your_own_secret_using_crypto

# Server Port
PORT=3000

# JWT Expiration
JWT_EXPIRES_IN=7d

# Node Environment (development, staging, production)
NODE_ENV=development

# ==================================================
# SECURITY NOTES:
# ==================================================
# 1. Never commit the actual .env file to git
# 2. Generate your own JWT_SECRET using the command above
# 3. Use different secrets for dev/staging/production
# 4. Keep your MongoDB credentials secure
# 5. Rotate JWT secrets periodically (every 6-12 months)
#
# To generate a secure JWT secret:
#   node -p "require('crypto').randomBytes(64).toString('hex')"
#   
# Or use the generator script:
#   node ../generate-jwt-secret.js
# ==================================================
